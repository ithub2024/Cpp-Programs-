# include<iostream>
# include <algorithm>
# include<string>
using namespace std;
int main(){
    string str = "Amitabh";

    //                          <string_name>.size();

            cout<<str.size()<<endl;
            cout<<str.length()<<endl;

    //                             push_back() & pop_back()

            // str.push_back(' ');
            // str.push_back('S');
            // str.push_back('h');
            // str.push_back('a');
            // str.push_back('r');
            // str.push_back('m');
            // str.push_back('a');
            // cout<<str<<endl;
            // str.pop_back();
            // str.pop_back();
            // str.pop_back();
            // str.pop_back();
            // str.pop_back();
            // str.pop_back();
            // str.pop_back();
            // cout<<str;


  //                           "+" OPERATOR (Star Concept)

            // string st = " Sharma";

            // str = str + st;
            // cout<<str<<endl;
            // st = st + " is great"; // post addition 
            // cout<<st<<endl;
            // st = "Good " + st;  // pre addition 
            // cout<<st<<endl;

  //                       reverse()

        // string ptr = "abcdef";
        // reverse(ptr.begin(),ptr.end()); //
        // cout<<ptr<<endl; //fedcba
        // reverse(ptr.begin()+2,ptr.end()); //abfedc
        // reverse(ptr.begin()+2,ptr.begin()+5);
        // cout<<ptr;
}